"use strict"; //treats all js codde as a newer version of js

//this can be run in a browser but not in node
/* alert("Your PC is infected 
with a virus. Click OK to remove the virus.") */

let name = "Rupankar" //String
let age = 20 //Number
let isApproved = true //Boolean
let state//Undefined
let temp = null //Null
//Symbol
//Object

console.log(typeof null); //Object
console.log(typeof undefined); // undefined